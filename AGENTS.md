# Grovix Connect

Keep the default branch in a working, shippable state.

Avoid rewriting published git history (force-pushing, rebasing, amending, or squashing commits that are already pushed) so collaborators retain a complete project history.
